<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.familyinstruction">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NoteMaterialActivity"
            android:label="@string/app_note_page_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EditorActivity" />

        <provider
            android:name=".data.InstructionProvider"
            android:authorities="com.example.android.familyinstruction"
            android:exported="false" />

        <activity
            android:name=".TextMaterialActivity"
            android:label="@string/app_material_page_name" />
        <activity
            android:name=".FamilyInstructionText"
            android:label="@string/family_instruction_of_yan" />
        <activity
            android:name=".FamilyInstructionVedio"
            android:label="@string/family_instruction_of_china" />
        <activity android:name=".TextDetailInfomation" />
        <activity android:name=".VedioDetailInformation" />
        <activity android:name=".MediaMaterialActivity"></activity>
    </application>

</manifest>